<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   <div class="container-fluid text-center">
     <div class="container px-5 my-5">
            <div class="display-6 text-center mt-1">Create new status</div>
            <div class="card mt-2 shadow">
                <%= form_with(model: @status, local:true) do |f| %>
                    <div class="card-header fw-bold">
                        <div class="col-4">
                            <div class="form-outline">
                                <%= f.label :date, class:"form-label text-center", placeholder:"Choose date" %>
                                <%= f.date_field :date,class: "form-control" %>
                            </div>
                        </div>
                    </div>
                    <h3 class= "text-center mt-1">Status details</h3>
                        <%= f.fields_for :tasks do |t| %>
                          <%= render 'task_fields', :f => t %>
                      <% end %>
                      <div>
                          <br/>
                          <%= link_to_add_association f, :tasks do %>
                             <button class="btn btn-primary" type="submit">Add task</button>
                          <% end %>
                      </div>
                    <div class="form-group text-center mt-4" style="margin-right: 82%;">
                        <%= f.submit "Send email",class: "btn btn-primary"%>
                    </div>
                <% end %>
         </div>
   </div>
   <script src="https://cdn.startbootstrap.com/sb-forms-latest.js"></script>
  </div>
  <%= render 'layouts/footer'%>
</body>
</html>

